import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.naqaa.vanapp',
  appName: 'التاجر المتنقل',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
